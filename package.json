{
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "mongoose": "^6.8.1"
  },
  "scripts": {
    "start": "node index.js",
    "build":"cd client &&  npm run build",
    "isntall-client":"cd client && npm install",
    "heroku-postbuild":"npm run install-client && npm run build"
  }
}
